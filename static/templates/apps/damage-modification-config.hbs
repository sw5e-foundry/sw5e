<form autocomplete="off">
    <h3>{{ localize "SW5E.DamageTypes" }}</h3>
    <p class="note">{{ localize "SW5E.DamageModification.Hint" }}</p>
    <ol class="trait-list modifications">
        {{#each modifications}}
        <li>
            <label>
                {{ label }}
                <input type="text" name="system.traits.dm.amount.{{ @key }}" value="{{ value }}"
            </label>
        </li>
        {{/each}}
    </ol>

    <h3>{{ localize "SW5E.DamagePhysicalBypass" }}</h3>
    <p class="note">{{ localize "SW5E.DamageModification.BypassHint" }}</p>
    {{> "sw5e.trait-list" choices=bypasses prefix="system.traits.dm.bypasses" }}

    <button type="submit" name="submit" value="1">
        <i class="fa-regular fa-save"></i> {{ localize "SW5E.TraitSave" }}
    </button>
</form>
