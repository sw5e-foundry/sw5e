<form id="ability-use-form">
    <p>{{title}}</p>
    <p class="notes">{{note}}</p>
    {{#each warnings}}
    <p class="notification error">{{this}}</p>
    {{/each}}

    {{#if (eq scaling "resource")}}
    <div class="form-group">
        <label>{{ localize "SW5E.ConsumeScalingLabel" }}</label>
        <div class="form-fields">
            <select name="resourceAmount">
                {{selectOptions resourceOptions selected=resourceAmount localize=true}}
            </select>
        </div>
    </div>
    {{/if}}

    {{#if (eq scaling "slot")}}
    <div class="form-group">
        <label>{{ localize "SW5E.PowerCastUpcast" }}</label>
        <div class="form-fields">
            <select name="slotLevel">
                {{#select slotLevel}}
                {{#each slotOptions as |l|}}
                <option value="{{l.key}}" {{disabled (not l.canCast)}}>{{l.label}}</option>
                {{/each}}
                {{/select}}
            </select>
        </div>
    </div>

    <div class="form-group">
        <label class="checkbox">
            <input type="checkbox" name="consumePowerSlot" {{checked consumePowerSlot}}>{{localize "SW5E.PowerCastConsume"}}
        </label>
    </div>
    {{/if}}

    {{#if (ne consumeUsage null)}}
    <div class="form-group">
        <label class="checkbox">
            <input type="checkbox" name="consumeUsage" {{checked consumeUsage}}>{{localize "SW5E.AbilityUseConsume"}}
        </label>
    </div>
    {{/if}}

    {{#if (ne consumeResource null)}}
    <div class="form-group">
        <label class="checkbox">
            <input type="checkbox" name="consumeResource" {{checked consumeResource}}>{{localize "SW5E.ConsumeResource"}}
        </label>
    </div>
    {{/if}}

    {{#if (ne createMeasuredTemplate null)}}
    <div class="form-group">
        <label class="checkbox">
            <input type="checkbox" name="createMeasuredTemplate" {{checked createMeasuredTemplate}}>{{localize "SW5E.PlaceTemplate"}}
        </label>
    </div>
    {{/if}}
</form>
